pipeline:
  name: java_project
  identifier: java_project
  projectIdentifier: agents
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: github_tony
        repoName: java_project
        build: <+input>
  stages:
    - stage:
        name: build
        identifier: build
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          steps:
            - step:
                name: explore_artifact_war_jar
                identifier: explore_artifact_war_jar
                type: Run
                spec:
                  shell: Sh
                  command: |
                    set -e; ls
                    # exit 1 (removed to fix pipeline failure)
            # ... other steps ...
